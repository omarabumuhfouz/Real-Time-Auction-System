// import { SellerDashboard } from "@/features/seller/components/seller-dashboard";

/**
 * Seller dashboard page — thin wrapper.
 * TODO: Import and render SellerDashboard from the seller feature.
 */
export default function Page() {
  return (
    <div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
      <h1 className="text-3xl font-bold tracking-tight">Seller Dashboard</h1>
      <p className="text-muted-foreground">
        Seller dashboard — TODO: wire up feature component
      </p>
    </div>
  );
}
