import { AccessManagementPage } from "@/features/admin";

export const metadata = {
  title: "Access Management | MazadZone Admin",
  description: "Manage system administrator credentials and dashboard credentials provisioning.",
};

export default function Page() {
  return <AccessManagementPage />;
}
